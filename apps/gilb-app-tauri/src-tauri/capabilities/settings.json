{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "settings",
  "description": "Capability for the settings window (label 'settings'). The app-defined commands it invokes (open_settings, get_openai_key, set_openai_key, test_openai_key) are auto-allowed for local origins because no app ACL manifest is declared; the window needs no extra IPC permissions.",
  "windows": ["settings"],
  "permissions": []
}
